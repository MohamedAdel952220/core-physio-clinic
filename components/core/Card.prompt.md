**Card** — soft white surface with hairline border + diffuse shadow; base container for content blocks.

```jsx
<Card variant="feature" interactive>…</Card>
<Card variant="glass">floating stat</Card>
```

Variants: `default`, `feature` (deeper shadow), `glass` (frosted, for floats over imagery), `tint` (mint fill). `interactive` adds hover-lift.
