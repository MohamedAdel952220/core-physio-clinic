**Logo** — Core Physio Clinic lockup (open-C ring + ascending motion dots + wordmark).

```jsx
<Logo variant="dark" size={40} />        // header
<Logo variant="light" size={44} />       // footer / teal sections
<Logo showText={false} size={32} />      // favicon / compact
```

Use `light` on teal or dark backgrounds. The mark alone reads as "Core" + upward movement.
